@echo off
REM Scheduler Test Script - Windows
REM Enable test job (runs every 2 minutes) to verify scheduler works correctly

echo.
echo Scheduler Enhancement Summary
echo ==============================
echo.
echo ^✓ Added job lock mechanism (prevents concurrent runs^)
echo ^✓ Added retry wrapper with exponential backoff (handles transient errors^)
echo ^✓ Added test job (runs every 2 minutes when enabled^)
echo.
echo To enable test job, set environment variable:
echo   set ENABLE_TEST_JOB=true
echo.
echo Then start the backend:
echo   npm run dev
echo.
echo You should see:
echo   1. "Test job (2-min interval) enabled" in logs
echo   2. "Test job executed" messages every 2 minutes
echo   3. New entries in admin dashboard Jobs tab -> test-job-2min
echo.
echo All jobs now protected with locks:
echo   - deactivate-expired-items (every minute^)
echo   - dedup-media (every minute^)
echo   - cleanup-tokens-analytics (daily at 3 AM^)
echo   - test-job-2min (every 2 minutes, when enabled^)
echo.
